# Archived one-bell fan-in (superseded 2026-09-25)

`fan-in.sh.superseded-2026-09-25` was the original D6.2 local file fan-in into an
append-only stream for the existing dead-man.

**Superseded because:** the one bus lives on the **rig**; D6.2 revised job is the
external heartbeat watcher on **controller** (`../watch.sh`). Do not enable this
archive as the default.
